# Server Configuration
PORT=3000
# SERVER_URL=http://localhost:3000

# Frontend URL for CORS (e.g., https://your-frontend.onrender.com)
FRONTEND_URL=http://localhost:5173

# Authentication
JWT_SECRET=your_super_secret_jwt_key_change_in_production

# PostgreSQL Database Configuration
# Option 1: Connection string (Used automatically by Render PostgreSQL)
# DATABASE_URL=postgresql://user:password@host:5432/roxiler_db

# Option 2: Individual variables (Local development)
DB_USER=smitinagulwar
DB_HOST=localhost
DB_NAME=roxiler_db
DB_PASSWORD=
DB_PORT=5432
DB_SSL=false
